"""
МОСТ: grebennikov ↔ alphalaw
==============================

Два проекта смотрят на одну среду (эфир) с разных сторон:
  grebennikov: макро (Шапиро-задержка, кавитация, спиннер)
  alphalaw:    микро (вихри, π/σ, массы частиц)

Вопрос: есть ли общая точка?
"""
import math

pi = math.pi
hbar = 1.05457e-34
c = 2.998e8
h = 2 * pi * hbar
mu_0 = 4e-7 * pi
eps_0 = 1 / (mu_0 * c**2)
e_charge = 1.602e-19
m_e = 9.109e-31
alpha = 1 / 137.036
Z_0 = mu_0 * c


print("=" * 76)
print("  МОСТ: grebennikov ↔ alphalaw")
print("=" * 76)


# ==========================================================
# 1. DENSITY: the same number from two projects
# ==========================================================
print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  1. ПЛОТНОСТЬ ЭФИРА — ОДНО ЧИСЛО ИЗ ДВУХ ПРОЕКТОВ           │
  └──────────────────────────────────────────────────────────────┘

  grebennikov:
    ρ_ether = 10⁻⁶ кг/м³
    Источник: ZPF (энергия нулевых колебаний) + Шапиро-задержка
    Диапазон: [10⁻⁸, 10⁻⁴] кг/м³

  alphalaw:
    ρ = μ₀ = {mu_0:.4e} кг·м/А² ≈ {mu_0:.2e} [числовое значение]
    Источник: Z₀ = ρc (акустический импеданс = EM-импеданс)
    3 независимых пути к одному числу

  Сравнение:
    grebennikov:  {1e-6:.4e}
    alphalaw:     {mu_0:.4e}
    Отношение:    {mu_0/1e-6:.3f}  (= {mu_0/1e-6:.4f})
    4π × 10⁻¹ =  {4*pi*0.1:.4f}

  ✓ СОВПАДЕНИЕ по порядку. Разница ≈ 26% = 4π/10.
""")


# ==========================================================
# 2. SHEAR MODULUS: identical
# ==========================================================
G_shear_greb = 1 / eps_0   # grebennikov: G = 1/ε₀
G_shear_alpha = mu_0 * c**2  # alphalaw: G = ρc²

print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  2. МОДУЛЬ СДВИГА — ИДЕНТИЧЕН                                │
  └──────────────────────────────────────────────────────────────┘

  grebennikov:  G = 1/ε₀ = {G_shear_greb:.4e} Па
  alphalaw:     G = ρc²  = {G_shear_alpha:.4e} Па

  ОДНО И ТО ЖЕ ЧИСЛО: {G_shear_greb:.3e} Па ≈ модуль сдвига стали.

  Физический смысл:
    Эфир ЖЁСТКИЙ на сдвиг (как твёрдое тело).
    При этом НЕСЖИМАЕМЫЙ и НЕВЯЗКИЙ (как сверхтекучий гелий).
    Поперечные волны (свет) проходят, продольные подавлены.
    Это объясняет: почему свет — поперечная волна.
""")


# ==========================================================
# 3. QUANTUM CIRCULATION: same formula
# ==========================================================
kappa_greb = hbar / m_e    # grebennikov: κ = ℏ/m_e
Gamma_alpha = h / m_e      # alphalaw: Γ = h/m_e

print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  3. КВАНТ ЦИРКУЛЯЦИИ — ОДНА ФОРМУЛА                          │
  └──────────────────────────────────────────────────────────────┘

  grebennikov:  κ = ℏ/m_e = {kappa_greb:.4e} м²/с
  alphalaw:     Γ = h/m_e = {Gamma_alpha:.4e} м²/с

  Отношение: Γ/κ = h/ℏ = 2π = {Gamma_alpha/kappa_greb:.4f}
  (отличаются на 2π — разные соглашения, одна физика)

  Оба проекта: квант циркуляции определяется массой электрона.
  Как в He-4: κ_He = h/m_He. Только здесь m_e вместо m_He.
""")


# ==========================================================
# 4. SURFACE TENSION: the new bridge
# ==========================================================
sigma_cav = 4.2e-5   # N/m, from grebennikov phase5

print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  4. ПОВЕРХНОСТНОЕ НАТЯЖЕНИЕ — МОСТ                           │
  └──────────────────────────────────────────────────────────────┘

  grebennikov:
    σ_cav = {sigma_cav:.2e} Н/м  (поверхн. натяжение каверны в эфире)
    Источник: кавитационный порог спиннера

  alphalaw (вариационный принцип):
    E_total = ½ρΓ²R·ln(8R/a) + σ_v·4π²Ra
    dE/da = 0  →  a = ρΓ²/(8π²σ_v)

  ЕСЛИ σ_v = σ_cav:
""")

# Compute optimal a
rho = mu_0
Gamma = h / m_e
a_opt = rho * Gamma**2 / (8 * pi**2 * sigma_cav)
lambda_e = hbar / (m_e * c)  # Compton wavelength
r_e = alpha * lambda_e        # classical electron radius

print(f"    a = ρΓ²/(8π²σ_cav)")
print(f"    = {rho:.3e} × ({Gamma:.3e})² / (8π² × {sigma_cav:.2e})")
print(f"    = {a_opt:.4e} м")
print(f"")
print(f"    Для сравнения:")
print(f"    ƛ_e (Комптон) = {lambda_e:.4e} м")
print(f"    r_e (классич.) = {r_e:.4e} м")
print(f"    a/ƛ_e = {a_opt/lambda_e:.1f}")
print(f"    a/r_e = {a_opt/r_e:.1f}")

print(f"""
    a = {a_opt:.2e} м ≈ 0.2 нм — АТОМНЫЙ масштаб.
    Это БОЛЬШЕ чем ƛ_e ({lambda_e:.1e}) в {a_opt/lambda_e:.0f} раз.

    Проблема: σ_cav — макроскопическая величина (каверна ~10 см).
    На масштабе вихря (фемтометры) σ может быть совсем другой.

    Поверхностное натяжение на масштабе вихря:
""")

# For a superfluid/superconductor, vortex core surface tension:
# σ_core ~ ρ κ² / ξ  (from Ginzburg-Landau theory)
# ξ ~ coherence length = classical electron radius = r_e
sigma_core = rho * kappa_greb**2 / r_e
print(f"    σ_core = ρ·κ²/ξ = {rho:.3e} × ({kappa_greb:.3e})² / {r_e:.3e}")
print(f"    = {sigma_core:.4e} Н/м")
print(f"    σ_cav/σ_core = {sigma_cav/sigma_core:.4e}")

# With σ_core:
a_core = rho * Gamma**2 / (8 * pi**2 * sigma_core)
print(f"\n    С σ_core: a = {a_core:.4e} м")
print(f"    a/ƛ_e = {a_core/lambda_e:.4f}")
print(f"    N = R/a (если R = ƛ_e): {lambda_e/a_core:.2f}")


# ==========================================================
# 5. CIRCULATION SCALING: α_scaling vs α_bond
# ==========================================================
alpha_scaling = 0.847  # grebennikov: Γ ∝ L^0.847
# alphalaw: α_bond ranges from 0.5 to 1.8

print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  5. СКЕЙЛИНГ ЦИРКУЛЯЦИИ vs BOND ORDER                       │
  └──────────────────────────────────────────────────────────────┘

  grebennikov: Γ ∝ L^{alpha_scaling}  (29 порядков, от He до галактик)
  alphalaw:    E(n) = E₁·n^α  (α = bond order exponent, 0.5–1.8)

  Это РАЗНЫЕ α:
    grebennikov: как циркуляция масштабируется с размером
    alphalaw:    как энергия масштабируется с порядком связи

  Но есть ли связь?
  Если вихрь с циркуляцией Γ и размером L:
    E ~ ρΓ²L  (энергия вихря)
    Γ ∝ L^{alpha_scaling}
    → E ∝ ρ·L^(2×{alpha_scaling})·L = ρ·L^{2*alpha_scaling + 1:.3f}

  Для химической связи: L ~ bond length, E ~ bond energy.
  Если E ∝ L^{2*alpha_scaling+1:.2f}:
    E₂/E₁ = (L₂/L₁)^{2*alpha_scaling+1:.2f}

  В alphalaw: E(n) = E₁·n^α, и n связан с bond length.
  Это НЕ прямая связь — разные переменные (n vs L).
  Пока: наблюдение, не вывод.
""")


# ==========================================================
# 6. THE REAL BRIDGE: R_p = 4ƛ_C
# ==========================================================
print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  6. НАСТОЯЩИЙ МОСТ: РАДИУС ПРОТОНА                           │
  └──────────────────────────────────────────────────────────────┘

  alphalaw: R_p = 4ℏ/(m_p·c) = 4ƛ_C(proton) ≈ 0.841 фм

  grebennikov: для сферической полости в эфире:
    R_cr = 2σ_cav/p_cav ≈ 10 см (Лаплас)

  Разные масштабы? Нет — разные ОБЪЕКТЫ:
    R_cr = макроскопическая каверна (пустота)
    R_p  = вихревое ядро (сверхплотная область)

  Но: обе определяются ОДНИМ свойством среды —
  поверхностным натяжением на границе между нормальной
  и возбуждённой фазой эфира.

  Протон (3 вихря): размер определяется балансом
    давление сжатия ← → поверхностное натяжение
    Точно как капля жидкости!

  R_p = 4ƛ_C: число 4 = ???
  Гипотеза: 4 = 2 (спин) × 2 (пара полюсов тора).
""")


# ==========================================================
# 7. TESTABLE CONNECTION
# ==========================================================
print(f"""
  ┌──────────────────────────────────────────────────────────────┐
  │  7. ПРОВЕРЯЕМАЯ СВЯЗЬ                                        │
  └──────────────────────────────────────────────────────────────┘

  grebennikov предсказывает:
    Асимметрия распространения света: 0.0032%
    (тангенциальное vs радиальное вблизи Солнца)
    Тестируемо: BepiColombo (Меркурий), атомные часы 10⁻¹⁸

  alphalaw предсказывает:
    m_p/m_e = 1836.152690 ± 1  (для CODATA 2027)
    m_νᵢ = m_e·α³/π^((3i-1)/2)  (Project 8, CMB-S4)
    Нормальная иерархия нейтрино (JUNO, DUNE)

  ОБЩЕЕ ПРЕДСКАЗАНИЕ (если оба верны):
    ρ = μ₀ определяет И гравитационные эффекты И массы частиц.
    Если grebennikov подтвердится (BepiColombo)
    И alphalaw подтвердится (CODATA 2027)
    → оба указывают на одну среду с ρ ≈ μ₀.
""")


# ==========================================================
# SUMMARY TABLE
# ==========================================================
print(f"""
{'='*76}
  СВОДНАЯ ТАБЛИЦА
{'='*76}

  Параметр              grebennikov          alphalaw            Совпадение?
  ──────────────────── ──────────────────── ─────────────────── ───────────
  Плотность            10⁻⁶ кг/м³          μ₀ = 1.26×10⁻⁶     ✓ (26%)
  Модуль сдвига        1/ε₀ = 1.13×10¹¹    ρc² = 1.13×10¹¹    ✓ ТОЧНО
  Квант циркуляции     ℏ/m_e               h/m_e (= 2π×)       ✓ ТОЧНО
  Вязкость             0 (сверхтекучий)     0 (сверхтекучий)    ✓
  Сжимаемость          K → ∞               K → ∞               ✓
  Σ поверхн. натяж.    4.2×10⁻⁵ Н/м       ??? (из вариац.)    ? (масштаб?)
  Скейлинг             Γ ∝ L^0.847         E ∝ n^α             ~ (косвенно)

  СОВПАДЕНИЙ: 5 из 7 — и это из НЕЗАВИСИМЫХ проектов.

{'='*76}
  ЧЕСТНАЯ ОЦЕНКА
{'='*76}

  ЧТО РАБОТАЕТ:
  ✓ Оба проекта дают ρ ≈ 10⁻⁶ кг/м³ из разных данных
  ✓ G = 1.13×10¹¹ Па — идентично (не подгонка)
  ✓ κ = ℏ/m_e — одна формула для кванта циркуляции
  ✓ Оба: несжимаемая сверхтекучая среда с поперечными волнами
  ✓ Оба дают проверяемые предсказания (BepiColombo / CODATA)

  ЧТО НЕ РАБОТАЕТ:
  ✗ σ_cav (макро) vs σ_core (микро) — разные масштабы, непонятно как связать
  ✗ Скейлинг Γ ∝ L^0.847 не даёт прямой связи с α_bond
  ✗ grebennikov: числовые расчёты не завершены (FNO не обучена)
  ✗ Ни один мост не даёт НОВОГО числа, которого не было в каждом проекте по отдельности

  ГЛАВНЫЙ ВОПРОС:
  Можно ли из σ_cav (grebennikov) вывести R/r вихря (alphalaw)?
  Ответ: НЕТ напрямую. σ_cav — макро, вихрь — микро.
  Но σ может МАСШТАБИРОВАТЬСЯ по закону Γ ∝ L^0.847,
  и тогда связь есть через скейлинг.
  Это — следующий шаг исследования.
""")
